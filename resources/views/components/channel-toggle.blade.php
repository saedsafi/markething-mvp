<label class="channel-toggle">
    <input type="checkbox" name="channels[]" value="{{ $value }}">

    <span>
        <strong>{{ $label }}</strong>
        <small>{{ $description }}</small>
    </span>
</label>